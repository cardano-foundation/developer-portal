# Scalus (on-chain) — coming soon

A Scalus implementation of the atomic swap validator will live here, so the same lesson can be
followed in Scala. For now the on-chain contract is implemented in Aiken (see `../aiken`).
