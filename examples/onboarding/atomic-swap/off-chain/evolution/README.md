# Evolution (off-chain) — coming soon

An Evolution (Lucid-evolution) implementation of the off-chain code + frontend will live here, so
the same lesson can be followed with a different SDK. For now the off-chain app is implemented with
Mesh (see `../mesh`).
