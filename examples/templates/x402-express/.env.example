# --- Facilitator ---------------------------------------------------------
# The hosted Cardano Foundation facilitator URL will be announced before the
# hackathon. Until then, run one locally (see README "Facilitator").
FACILITATOR_URL=http://localhost:4022

# --- Seller --------------------------------------------------------------
# The preprod address that receives payments (addr_test1...).
SELLER_ADDRESS=
SELLER_PORT=4021

# --- Buyer ---------------------------------------------------------------
# Run `npm run wallet` to generate these, then fund via the faucet.
MNEMONIC=
BLOCKFROST_PROJECT_ID=
# BLOCKFROST_BASE_URL=https://cardano-preprod.blockfrost.io/api/v0
# SELLER_URL=http://localhost:4021
