---
title: "Off-chain: Atomic Swap"
sidebar_label: "Off-chain: Atomic Swap"
description: Build and submit the atomic swap transaction with off-chain code.
---

import CodeBlock from "@theme/CodeBlock";
import extractRegion from "@site/src/utils/extractRegion";
import BuildTx from "!!raw-loader!@site/examples/onboarding/atomic-swap/build-tx.ts";

# Off-chain: Atomic Swap

## Goal

Build the off-chain transaction that performs the atomic swap and submit it to a test network.

The off-chain code lives in the repo at [`examples/onboarding/atomic-swap/build-tx.ts`](https://github.com/cardano-foundation/developer-portal/blob/staging/examples/onboarding/atomic-swap/build-tx.ts) and is imported here, piece by piece, so it can never drift from the code that is tested.

First, the shapes we're working with. A `Party` is who is swapping what, and a `SwapDraft` is the inputs and outputs of the transaction:

<CodeBlock language="ts" title="build-tx.ts — types">
  {extractRegion(BuildTx, "types")}
</CodeBlock>

Now the swap itself. Notice each party's output receives the *other* party's asset — that's what makes it a swap, and doing it in one transaction is what makes it atomic:

<CodeBlock language="ts" title="build-tx.ts — buildAtomicSwapDraft">
  {extractRegion(BuildTx, "build")}
</CodeBlock>

<!-- To show the entire file at once instead, import it whole:
<CodeBlock language="ts">{BuildTx}</CodeBlock> -->

## See the result

<!-- TODO: a transaction hash to open in a chain explorer. -->

## Go deeper

<!-- TODO: link to /docs/developers/curriculum/start-building/transaction-building and the next step. -->
